// Query parameter that opens AppSettingsModal on a given tab from a URL
// (e.g. ?settings=billing on a Stripe return URL). Read and stripped by the
// router, see computeSettingsTabRequest.
export const SETTINGS_QUERY_PARAM = "settings"
