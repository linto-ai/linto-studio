import { Node } from '@tiptap/core';
/**
 * Custom Document node that only accepts `turn` blocks.
 * Replaces TipTap's default `doc` node.
 */
export declare const TranscriptionDocument: Node<any, any>;
