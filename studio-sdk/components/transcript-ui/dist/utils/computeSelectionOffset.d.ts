/** Character offset of the current selection focus inside `container`'s plain
 *  text, or null when the selection lives elsewhere. */
export declare function computeSelectionOffset(container: HTMLElement): number | null;
