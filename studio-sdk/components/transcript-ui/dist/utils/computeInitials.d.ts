/** Initials shown in a user avatar: first letter of the first two words,
 *  uppercased — "Marie Dupont" → "MD", "marie" → "M", "" → "?". */
export declare function computeInitials(name: string): string;
