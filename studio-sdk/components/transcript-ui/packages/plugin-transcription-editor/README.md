# @linto/transcript-ui-plugin-transcription-editor

Collaborative editing of turns and speakers: rename, split, merge, and reassign, with per-turn locks and server-authoritative sync (the host applies broadcasts from other editors; this plugin never computes retimed truth itself).
