import { defineConfig } from "vite"
import vue from "@vitejs/plugin-vue"

// Dev-only playground — exercises every package together. Never published.
export default defineConfig({
  plugins: [vue()],
})
