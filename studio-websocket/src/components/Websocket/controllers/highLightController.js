import Conversations from "../models/conversations.js"
import { v4 as uuidv4 } from "uuid"

export default async function updateConversationController(scope) {
  try {
    // verify if job is running
    // if not running
    // request highlight
    // start job
    
  } catch (error) {
    this.emit("error")
  }
}
